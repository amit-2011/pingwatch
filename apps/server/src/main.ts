/**
 * @pingwatch/server — the runtime host. The real NestJS bootstrap, the `pingwatch` CLI
 * (`bin → dist/cli.ts`), and the embedded Next.js dashboard arrive in T4/T5.
 *
 * This placeholder keeps the app buildable until then.
 */
export function bootstrapPlaceholder(): string {
  return '@pingwatch/server';
}
