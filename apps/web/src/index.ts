/**
 * @pingwatch/web — Next.js 15 dashboard + public status pages, built `output:'standalone'`
 * and embedded into the Nest process. The Next.js app (App Router, shadcn/ui, Tailwind)
 * arrives in T5.
 *
 * This placeholder keeps the package buildable until then.
 */
export const WEB_PACKAGE = '@pingwatch/web' as const;
