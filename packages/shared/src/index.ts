/**
 * @pingwatch/shared — the single strict wire contract shared by backend and frontend:
 * union constants, zod schemas, DTOs, the `MonitorType` + `NotificationProvider`
 * plugin interfaces, and the error envelope.
 *
 * Real contents arrive in T2. This placeholder keeps the package buildable.
 */
export const SHARED_PACKAGE = '@pingwatch/shared' as const;
