/**
 * @pingwatch/db — Prisma schema (one canonical model, SQLite + Postgres datasource folders),
 * both generated clients, and a runtime selector that picks the client by DATABASE_URL.
 *
 * Real schema + generated clients arrive in T3. Placeholder keeps the package buildable.
 */
export const DB_PACKAGE = '@pingwatch/db' as const;
